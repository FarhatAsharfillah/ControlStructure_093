# Input untuk mencari angka ganjil
def tampilkan_angka_ganjil(n):
# Menggunakan loop untuk iterasi dari 1 hingga n
    odd_numbers = [i for i in range(1, n+1) 
# Memeriksa apakah i adalah angka ganjil                
    if i % 2 != 0]
    print (odd_numbers)

# Input nilai kemudian konversi ke interger
n = int(input("Enter the value of n: "))
# Tampilkan angka ganjil dengan print dengan output "Angka ganjilnya"
print("Angka ganjilnya adalah:", n, tampilkan_angka_ganjil(n))
